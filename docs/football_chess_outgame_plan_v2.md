# Football Chess アウトゲーム整理v2 実装計画書（マイページ統合版）

> このドキュメントは `docs/football_chess_outgame_plan.md`（速い層/こだわる層の初版）の後継。
> 初版のPhase 1・2（前回設定の永続化・TeamSelect廃止・DifficultySelect統合）は実装済みのため対象外。
> 本書は「自分のチームがどれか・誰と戦うか分からない」という体験課題に対する再設計を扱う。
> 迷ったら §1 設計意図に戻り、作業は §4 実装計画の単位で進める。
>
> **進捗（2026-07-01）**: Phase 1〜6（T1〜T14）すべて実装・コミット済み（詳細はCLAUDE.md実装済み仕様表を参照）。
> 型チェック・テストスイート（714件）green。ブラウザでの実機能確認（UI目視）・オンライン対戦/フレンド対戦のE2E動作は未検証。
> 直近: Phase6 T13（コレクション画面リメイク、`3d0e4b1`）・T14（戻るボタン統一、`d26ffcb`）を実装完了。

---

## 0. このドキュメントの位置づけ

- 対象: アウトゲーム（マイページ〜対戦開始まで）の再設計
- ゴール: **「自分のチームがどれか」「誰と戦うか」を常に一目で分かる状態にする**
- 前提: 必要な素材（NPC_TEAMS のエンブレム/名前/Era、編成、ショップ）はすでに実装済み。**新規実装ではなく「配線の組み替えと表示の追加」が中心**
- 既存仕様を削ってよい: `TeamSelect` 相当の再導入はしない。旧「クイックマッチ／モード選択／プリセットチーム」の3入口分立は本書で解消する

---

## 1. 設計意図（最重要）

### 1.1 「自チーム」を一級市民にする

現状の最大の問題は、**プレイヤーが今どのチームを使っているのか、どこにも表示されていない**こと。
`Formation.tsx` はコマの寄せ集めを組む画面でしかなく、チーム名もエンブレムもない。
一方 `src/data/npc_teams.ts` にはすでに名前・絵文字・Era・フォーメーション名が揃っている（`PresetTeamsScreen.tsx` でしか使われていない）。

→ **マイページ**を新設し、「今の自チーム」を常時1枚のカードとして可視化する。ショップ（選手購入）・編成（スタメン+配置）はすべてこのマイページから出入りする寄り道として位置づける。

### 1.2 「対戦相手」も一級市民にする

COM対戦の相手は `createDefaultAwayPieces()` で常に同じ無名の4-4-2固定チームが生成されており、対戦相手の名前もエンブレムも一切表示されない。

→ COM対戦時は `NPC_TEAMS` から相手を選出し、対戦開始前に「対戦相手：⚡️〇〇（Era3）」を必ず見せる。

### 1.3 対戦は3種類に整理する

| 種別 | 内容 |
|---|---|
| COM対戦 | 難易度選択 + 対戦相手プレビュー（NPC_TEAMSから選出） |
| オンライン対戦 | ランク/カジュアル。マッチング人数が不足する場合はBotで補完 |
| フレンド対戦 | 招待コード／URLを発行し、コピペで相手に共有。相手はコード/URL入力で参加 |

COM観戦（COM同士の自動対戦）は上記3種とは別枠の小さい機能として残す（動作確認用途）。

---

## 2. 現状の動線（整理前）

```
タイトル
 ├─「前回の編成で対戦」──────▶ マッチング
 ├─「モードを選んで対戦」──▶ モード選択（ranked/casual/com/comVsCom + 難易度）
 │                              ├─「編成して開始」──▶ 編成
 │                              └─「この設定で開始」──▶ マッチング
 ├─「編成をいじる」────────▶ 編成
 ├─「フレンド対戦」────────▶ FriendMatchScreen（モック・API未接続）
 └─「プリセットチーム」────▶ プリセット選択 → 即COM対戦開始（モード選択を経由しない隠れ導線）
```

### 洗練を妨げている主因

1. **試合を始める入口が3つに分立**（クイックマッチ／モード選択／プリセットチーム）。相互の関係が画面上どこにも説明されない
2. **「自分のチーム」に見た目のアイデンティティがない**。チーム名・エンブレムがどこにも出ない
3. **COM対戦の相手が常にブラックボックス**。`createDefaultAwayPieces()` が常に同じ無名チームを生成
4. **フレンド対戦がモックのまま公開導線に出ている**
5. タイトルのボタンが10個（メイン4＋サブ6）あり、全体像が一望しづらい

---

## 3. 整理後の動線（正本）

```
[マイページ（自チームハブ）]
   自チームカード（エンブレム・チーム名・スタメン概要）
   ├─「ショップへ」──▶ [ショップ]（選手購入）─┐
   ├─「編成する」────▶ [編成]（スタメン+配置、1画面完結）─┤
   │                                                    └─▶ マイページへ戻る（カード更新）
   └─「対戦へ」──▶ [対戦タイプ選択]
                     ├─ COM対戦 ──▶ 難易度選択+相手プレビュー ──▶ [マッチング]
                     ├─ オンライン対戦 ──▶ ランク/カジュアル ──▶ [マッチング]（Bot補完あり）
                     ├─ フレンド対戦 ──▶ 招待コード/URL発行 or 参加 ──▶ [マッチング]
                     └─ COM観戦（小さく配置） ──▶ [マッチング]

[マッチング] （自チーム vs 対戦相手を名前・エンブレム表示）──▶ [対戦] ──▶ [リザルト]

[リザルト] ─┬─「もう一度」────▶ [マッチング]
            ├─「編成を変える」─▶ [編成]
            └─「マイページへ」──▶ [マイページ]

[補助機能]（マイページから随時アクセス、試合フローとは別軸）
   コレクション / ランキング / プロフィール / リプレイ / 設定
```

### 変更点サマリ

| 変更 | 内容 | 効果 |
|---|---|---|
| マイページ新設 | `Title.tsx` を「自チームカード」中心のハブに再設計 | 自分のチームが常に見える |
| チーム識別情報の追加 | 自作編成にも名前/代表アイコンを付与（プリセットは既存の name/emoji を流用） | 「今このチームを使っている」が一目瞭然 |
| 対戦相手の可視化 | COM戦は `NPC_TEAMS` からランダム選出し、開始前にプレビュー表示 | 「誰と戦うか」が試合前に分かる |
| 対戦タイプの3分類 | COM対戦／オンライン対戦／フレンド対戦を並列の選択肢に統合 | 入口の分立を解消 |
| オンラインへのBot補完 | マッチング人数不足時にBotを割り当てる | 待ち時間短縮 |
| フレンド対戦の実装 | 招待コード/URL発行→コピペ共有、参加側はコード/URL入力 | モックから実機能へ |
| プリセットチーム画面の位置づけ変更 | 独立導線を廃止し「編成」内の選択肢（自作 or プリセットから選ぶ）に統合 | 入口の重複解消 |
| COM観戦の格下げ | 対戦タイプ選択内に小さく残す（動作確認用） | 表に出しすぎない |

---

## 4. 実装計画（Claude Code 作業単位）

> 既存はフルスペック。多くは「既存画面の再配置・表示追加」。対象ファイルは現状把握用の目安。

### Phase 1: 自チームの可視化基盤（最優先）

**T1. チーム識別情報の型定義**
- `LastSetup`（`src/client/utils/lastSetup.ts`）に `teamName` / `teamEmoji` / `origin`（'custom' | 'preset'）を追加
- 自作編成時は `formation.teamName` を保存できるようにし、未設定時は「マイチーム」をデフォルト表示
- プリセット選択時は `PresetTeam.name` / `emoji` をそのまま引き継ぐ
- 受け入れ条件: 編成・プリセットどちらで確定しても、名前とエンブレムが確定する

**T2. マイページ画面の新設（`Title.tsx` を置き換え）**
- 起動直後に「自チームカード」（エンブレム・チーム名・スタメン人数/コスト合計）を最上段に常時表示
- カードから「ショップへ」「編成する」「対戦へ」の3導線
- 受け入れ条件: 起動して最初に見える画面で「今のチーム」が一目で分かる

**T3. COM対戦相手の可視化**
- `battleUtils.ts` の `createDefaultAwayPieces()` を廃止し、`NPC_TEAMS` からランダム（または難易度連動）で1チーム選出する関数に置き換え
- 選出結果を対戦タイプ選択画面・マッチング画面に「対戦相手：{emoji}{name}（Era{n}）」として表示
- 受け入れ条件: COM対戦開始前に必ず相手のチーム名とエンブレムが見える

### Phase 2: 対戦タイプの再編

**T4. 対戦タイプ選択画面（`ModeSelect.tsx` 改修）**
- 「COM対戦／オンライン対戦／フレンド対戦」を並列カードで提示し、COM観戦は下部に小さく配置
- COM対戦選択時のみ難易度＋相手プレビュー（T3）を展開
- オンライン対戦選択時のみランク/カジュアルを展開
- 受け入れ条件: 3種の対戦タイプが同一画面で選べ、選択に応じて必要な設定だけ表示される

**T5. オンライン対戦のBot補完**
- マッチメイキングDO（`durable/matchmaking.ts`）に、一定時間人が見つからない場合Botを割り当てるフォールバックを追加
- 受け入れ条件: オンライン対戦で対戦相手が見つからない場合も一定時間内に試合が開始する

**T6. フレンド対戦の実装（`FriendMatchScreen.tsx` のモック解消）**
- 招待側: 招待コード（短い英数字）とマッチURLを発行するAPIを新設し、コピペ用に画面表示
- 参加側: コードまたはURL（クエリパラメータ経由）で該当セッションに合流
- 受け入れ条件: 招待コード/URLをコピペで共有し、相手がそれだけで合流できる

### Phase 3: 整理・削減

**T7. 旧3入口の統合**
- タイトルの「クイックマッチ／モード選択／プリセットチーム」ボタンを廃止し、マイページの「対戦へ」1本に集約
- 受け入れ条件: 対戦を始める入口がマイページの「対戦へ」のみになる

**T8. プリセットチーム画面の位置づけ変更**
- `PresetTeamsScreen.tsx` への独立導線を廃止し、「編成」画面内の選択肢（自作編成 / プリセットから選ぶ）として統合
- 受け入れ条件: プリセットチームは「編成する」経由でのみアクセスでき、独立ボタンが消える

---

### Phase 4: ビジュアルの主役化（参考: grassrootsfootball.football との比較）

**T9. マイページ・対戦タイプ選択の視覚的リメイク**
- 背景差分（2026-07-01）: `https://grassrootsfootball.football/` との比較で、情報設計は近いが「視覚的な主役の作り方」に差があると判明。具体的に4点：
  1. 自分のアイデンティティ（アバター/エンブレム）が画面中央に大きく主役として置かれている
  2. メイン導線が「PLAY NOW」「ONLINE MATCH」の巨大2択ボタンに絞られている
  3. サブ機能（Customize/Room List/Club/Shop/Profile/Settings）が3列×2行の整ったグリッドで畳まれている
  4. 黒×黄色の強いコントラストで「ゲームを開いた」感が一発で伝わる
- 対応方針:
  - **T9a. 自チームカードの格上げ**: `MyPage`（旧Title.tsx）の自チームカードを、エンブレム絵文字＋テキストの小さいサブラベルから、画面中央の大きめビジュアルカード（エンブレム/代表アイコンを主役サイズで表示）に変更
  - **T9b. 対戦導線の2択化**: T4で作った「COM対戦／オンライン対戦／フレンド対戦」の3枚並列カードを、「今すぐCOM対戦」「オンライン対戦」の2大ボタンに再編し、フレンド対戦はその下に控えめな3番目の選択肢として配置（COM観戦はさらに小さく最下部）
  - **T9c. 補助機能のグリッド化**: コレクション/ランキング/プロフィール/リプレイ/設定/ショップの6つを、横並びpillの`flex-wrap`から3列×2行の均等グリッドに変更
  - **T9d. 配色コントラスト強化**: アクセントカラー（現状の`#ffd700`系グラデーション）の主張を強め、ベース配色とのコントラストを上げる
- 受け入れ条件: マイページ起動直後に「自分のチーム」が画面の主役として視認でき、対戦への主導線が2つの巨大ボタンに絞られ、補助機能が整ったグリッドで表示される

---

### Phase 5: ワンクリック対戦の再設計（退行の修正 + ターゲット再定義）

**T11. マイページに「ワンクリック対戦」を導入**

- 背景（2026-07-01、実プレイで発覚）: 初版計画（`football_chess_outgame_plan.md`）のPhase 1で実装されていた
  「前回の編成で対戦」ワンタップ機能が、T7（旧3入口統合）の際に**巻き添えで完全に削除されていた**
  （`App.tsx`の`handleQuickMatch`ごと消滅。`lastSetup`のデータ自体は保存され続けている）

- **設計の検討過程（重要）**: 当初は初版と同じ「前回の編成で対戦」をそのまま復活させる案だったが、
  検討の結果**却下**。理由: このボタンの主なターゲットは「返ってきた編成済みプレイヤー」ではなく、
  **初めて来て・深く考えずに押す人**。この層には「前回」が存在しない上、
  `Formation.tsx`の初期状態（`createInitialOwned()`）は「GK」「MF 1」のような名無し仮コマで
  スタメンも0人。「前回と同じ」を素朴に実装すると、初めての人には空っぽ/味気ないチームが渡ってしまう。
  一方でゲームには`npc_teams.ts`（7時代分、名前・エンブレム付きのNPCチーム）という
  「顔のある」資産が既にあり、プリセットチーム選択でしか使われていない。
  → **ワンクリック対戦は効率化ボタンではなく、「この世界観・キャラの魅力を最短距離で1回体験させる入口」として設計する**

- **確定した設計**:
  - **編成済み**（`lastSetup.formationData`が存在する = 一度でも編成を確定した）プレイヤー
    → 従来通り「前回の編成で対戦」。自分の努力を無駄にしないことを優先し、そのままMatchingへ直行
  - **未編成**（`lastSetup.formationData`が存在しない = 初めて、または編成に触れたことがない）プレイヤー
    → ボタンラベルは「今すぐ対戦」等に変更。`NPC_TEAMS`（7時代）から**毎回ランダムに1チームを自チームとして割り当てて**Matchingへ直行
    → 対戦相手（T3で実装済みのCOM相手選出）とは別チームになるよう、同一チームの重複だけ避ける
    → 毎回違うチームに当たることで「今回は⚡〇〇だった」という気づきを生み、編成への興味の種にする
  - **試合後の軽い誘導**: 未編成プレイヤーが「今すぐ対戦」を使った場合、リザルト画面に
    「気に入ったチームがあれば編成してみる？」等の**押し付けないバナー**を表示（編成済みプレイヤーには出さない）
  - 「対戦へ」ボタンは従来通り対戦タイプ選択画面へ（設定を変えたい時用として維持）

- 受け入れ条件:
  - 未編成の初回プレイヤーが、マイページから1クリックで「顔のあるNPCチーム」を使ったCOM対戦に入れる
  - 編成済みプレイヤーは、1クリックで前回の編成のまま対戦に入れる
  - 未編成プレイヤーがワンクリック対戦を使った試合後、編成への軽い案内が表示される

---

### Phase 5続き: トップページ（マイページ）にCOM対戦・ランダム対戦を直接配置

**T12. マイページ最上位を「1画面で完結」させる（Grassroots比較の未消化分の解消）**

- 背景: T9bで狙った「PLAY NOW/ONLINE MATCHの2大ボタンを1画面に」は、
  実際にはマイページではなく1つ奥の対戦タイプ選択画面（ModeSelect）にしか実装されていなかった。
  マイページには「今すぐ対戦」（COM相当）と「対戦へ」（選択画面への遷移）しかなく、
  **オンラインのランダム対戦がトップページから直接押せない**状態だった
- あわせてT11で見つかった不具合も本タスクで解消する:
  「前回の編成で対戦」が`lastSetup.gameMode`をそのまま引き継ぐため、
  前回オンラインで遊んでいるとワンクリック対戦のはずが待機マッチングに化けてしまう

- **確定した設計**:
  - マイページ最上位に、同格の大ボタンを2つ配置する
    1. **「COM対戦」**: 常に`mode='com'`固定でMatchingへ直行（T11のロジックを流用、
       gameModeは`lastSetup`から引き継がず常にCOM固定にする＝T11のバグ修正を兼ねる）
       編成済みならその編成、未編成ならランダムNPCチームを使用（T11の分岐はそのまま維持）
    2. **「ランダム対戦」**: オンライン対戦（カジュアル相当）のマッチングへ直行。
       ModeSelectのランク/カジュアル選択を経由しない。Bot補完（T5）により極端に待たされることは想定しにくいが、
       性質上「即座」は保証できない旨をボタン下に小さく注記してもよい（例:「対戦相手を探します」）
  - ログイン状態表示（`AuthStatusBar`）は既存のまま、視認性（コントラスト/サイズ）を強化
  - 「対戦へ」は、フレンド対戦・ランク戦・COM観戦・難易度変更など**あえて選びたい人向け**の
    控えめなリンクとして残す（既存のModeSelect自体は削除しない）
  - 編成/ショップは現状の小さめボタンのまま維持

- 受け入れ条件:
  - マイページを開いた瞬間に、COM対戦・ランダム対戦・ログイン状況のすべてが
    スクロールや追加クリックなしで視認・実行できる
  - 「COM対戦」ボタンは、前回のモードに関わらず常に即座にCOM対戦へ入る
  - 「ランダム対戦」ボタンは、対戦タイプ選択画面を経由せず直接オンラインマッチングへ入る

---

### Phase 6: 周辺画面の底上げ — ✅ 完了（2026-07-01, T13: `3d0e4b1` / T14: `d26ffcb`）

**T13. コレクション画面の視覚的リメイク**

- 現状の問題: `CollectionScreen.tsx`はマイページ(T9)の視覚言語（紺〜黒グラデーション背景+金のグロー枠+グリーンのアクセント）を一切引き継いでおらず、素のダークUIのまま。ヘッダーはプレーンテキスト、フィルターは10px文字が密集したピル、未所持コマは無地の灰色丸。200人×7時代というこのゲームの資産が全く映えていない
- デザイン方針（マイページと世界観を統一しつつ、「図鑑」らしい構造を持たせる）:
  - **ヘッダーをヒーロー化**: 「COLLECTION」のプレーン表示ではなく、所持数（例: 127/188）を大きく主役として見せる。進捗リング/バーを添えてもよい
  - **Era別のセクション構成**: 現状の1枚岩グリッドを、7時代ごとの見出し付きセクション（棚）に分ける。`era_shelf`データは既にあるので構造化はデータ的に自然。「時代を巡る図鑑」という体験にする
  - **カードの質感向上**: コマ枠をマイページのカードと同じグロー処理に。未所持は現状の無地灰色丸ではなく、シルエット/鍵アイコンなど「持っていないことに意味がある」表現に変更
  - **フィルターの再設計**: 機能は維持しつつ、10px詰め込みではなくタップしやすいサイズに（マイページのボタン言語に揃える）
  - 詳細モーダルも同様のトーンに統一
- 受け入れ条件:
  - [x] コレクション画面がマイページと同じ世界観（配色・カードのグロー感）で統一され、
    Era別に構造化された「図鑑」として見える
- **実装メモ**: 背景をマイページと同じ`linear-gradient(180deg, #000000 0%, #14142c 55%, #000000 100%)`に変更。
  ヘッダーは所持数（例: 127/188）を`#ffd700`で大きく表示+進捗バー。`NPC_TEAMS`の`name_ja`から
  `ERA_LABELS`（時代名、「オールスター」を除去して導出）を組み立て、Era 1〜7の見出し付きセクションに分割。
  所持コマは金グロー枠（`rgba(255,214,0,0.25)`border+glow）、未所持は🔒アイコンの点線円に変更。
  フィルターボタンをタップしやすいサイズに拡大。i18nキー+2件（`collection.title`/`era_label`、全7言語パリティ維持）。
  **ブラウザでのUI目視確認は未実施**（本環境では検証不可）。

**T14. 「戻る」ボタンの位置・スタイルを統一**

- 現状の問題（画面ごとに実装がバラバラ、共通コンポーネント化されていない）:
  - 大半の画面: コンテンツ末尾にセンター配置の単独ボタン（Collection/Profile/Settings等）
  - `RankingScreen`: ボタン自体に個別marginがあり余白が微妙に異なる
  - `ShopScreen`: ローディング表示直後に`marginTop:8`でアドホック配置
  - `ReplayScreen`: 空状態と再生中で全く別のボタン実装（前者は完全中央寄せ、後者は`CtrlBtn`）
  - `Formation.tsx`: 使われていない`onBack`propが残存（デッドコード、実害はないが要削除）
- 対応方針:
  - 共通コンポーネント`BackButton`（仮称）を新設し、1つの正本スタイル・配置ルールに統一する
    （例: 画面下部、`padding:'12px 0'`+`textAlign:'center'`の枠内、ボタンは`padding:'8px 24px'`/`border:1px solid rgba(255,255,255,0.15)`/`borderRadius:8`/`color:#888`/`fontSize:14`）
  - 対象: CollectionScreen / ProfileScreen / RankingScreen / SettingsScreen / ShopScreen / ReplayScreen（空状態）/ FriendMatchScreen / PresetTeamsScreen / ModeSelect
  - `ReplayScreen`の再生中`CtrlBtn`は遷移先が異なる（'result'へ）ため機能は残すが、見た目のトーンは`BackButton`に揃える
  - `Formation.tsx`の未使用`onBack`propを削除
- 受け入れ条件:
  - [x] どの画面でも「戻る」が同じ見た目・同じ位置（画面下部中央）にあり、押した時の余白のガタつきがない
- **実装メモ**: `src/client/components/ui/BackButton.tsx`を新設（`onClick`/`label?`のみ受け取る単純コンポーネント、
  `padding:'12px 0'`+`textAlign:'center'`の枠内に`padding:'8px 24px'`/`border:1px solid rgba(255,255,255,0.15)`/
  `color:#888`/`fontSize:14`ボタン）。CollectionScreen/ProfileScreen/RankingScreen/SettingsScreen/ShopScreen/
  ReplayScreen（空状態）/FriendMatchScreen（`handleBack`のカスタム遷移も`onClick`でそのまま渡せる）/
  PresetTeamsScreen（画面下部のみ、モーダル内の対アクションボタンは対象外）/ModeSelectに適用。
  ReplayScreenの再生中`CtrlBtn`（遷移先が'result'固定で異なる）は機能維持しつつトーンのみ既存で一致することを確認。
  `Formation.tsx`の未使用`onBack`propを削除。

- **追記（2026-07-01, `b8f3184`）: BackButtonがコンテンツと一緒にスクロールする不具合を修正**
  - 問題: `ProfileScreen` / `SettingsScreen` / `ShopScreen`は外側コンテナが`minHeight:'100%'`+`overflowY:'auto'`
    のままだったため、コンテンツが長いとBackButtonごと一緒にスクロールし、画面下部に固定されず見えなくなっていた
    （ProfileScreenで実際に発生・報告された）。`CollectionScreen`/`RankingScreen`は当初から
    `height:'100%'`固定+内側`flex:1, overflowY:'auto'`の構造だったため対象外
  - 修正: 3画面を`CollectionScreen`/`RankingScreen`と同じ構造に統一
    - 外側コンテナ: `height:'100%'`（固定、overflowはコンテナ自体には付けない）
    - 中身のスクロール可能な部分だけ: `flex:1, overflowY:'auto'`でラップ
    - `BackButton`はそのスクロール領域の外（画面下部）に固定配置
  - `ShopScreen`は特にヘッダー（タイトル+インゴット残高）/説明文/ポジションフィルターをスクロール領域の外に固定し、
    コマカタログのグリッドのみをスクロール対象にした
  - 型チェック・テストスイート（714件）green。**ブラウザでのUI目視確認は未実施**（本環境では検証不可）

---

## 5. 未決事項（要・意思決定）

- [ ] COM対戦の相手選出はランダムか、難易度に応じてEra/強さを連動させるか
- [ ] 自作編成のチーム名は自由入力か、候補から選ぶ形にするか
- [ ] フレンド対戦にBot要素は不要という理解でよいか（招待制のため基本的に不要と想定）
- [ ] オンライン対戦のランク/カジュアルの違いを対戦タイプ選択画面でどこまで説明するか
- [ ] マイページの「自チームカード」をタップした時、ショップ/編成のどちらに飛ぶかの優先度（現状は横並び2ボタン想定）

---

## 6. 用語

- **マイページ**: 自チームカードを中心としたアウトゲームのハブ画面（旧タイトルを置き換え）
- **自チームカード**: エンブレム・チーム名・スタメン概要を表示する常設UI
- **対戦タイプ**: COM対戦／オンライン対戦／フレンド対戦の3分類（+ COM観戦は別枠）

---

## 7. 既知の問題

### スマホ実機（Android Chrome）: KICKOFF後のタッチで暗転・進行不能（未解決、優先度: 高）

- **症状**: footballchess.io をスマホ実機（Android Chrome）で開き、COM対戦のKICKOFF表示後に画面をタッチすると暗転して進行不能になる。PC（同一ビルド）では発生しない。
- **試したが直らなかった修正（`61085c0`、2026-07-02）**:
  - ターン再生チェーン（Battle.tsx の浮いたasync IIFE）への `.catch()` フェイルセーフ（例外時に演出状態リセット + NEXT_TURN で復旧）
  - スマホ自動ズーム（HexBoard.tsx §2-6, 2.5x）のパン範囲クランプ + rect縮退時スキップ
  - → **例外系のフェイルセーフで直らなかった = 原因は例外ではなく描画系/実機固有の可能性が高い**
- **再現状況**: Playwrightのモバイルエミュレーション（Pixel 7/Chromium・iPhone 13/WebKit、dev/prodビルド、本番footballchess.io直接、チュートリアル有無、reduced-motion）では再現しない。コンソールエラー・unhandled rejectionも検出されず。回帰テストは `node e2e/mobile_battle_failsafe.mjs`（要devサーバー）。
- **次の調査手段**: Android実機をUSB接続し、PC Chromeの `chrome://inspect` からリモートデバッグでコンソール・DOM・レイヤー状態を確認しながら再現する。フェイルセーフ導入済みのため、例外系なら `[Battle] replay chain error` ログが出るはず（出なければ描画系で確定）。
- **サービスイン前に必ず解決すること。**

### オンライン対戦/フレンド対戦のE2E未検証 → ✅ 解消（2026-07-02、2クライアント実接続で検証済み）

CLAUDE.mdに記録されていた検証負債「オンライン対戦・フレンド対戦のE2E動作は一度も検証されていない」を解消した。
ローカル検証環境（wrangler dev --local + vite + テスト用JWKS/JWT基盤 `e2e/online/authkit.mjs`、本番認証情報は不使用）で
Playwrightの独立2ブラウザコンテキスト（プレイヤーA/B）を接続し、以下5シナリオを自動テスト化して全PASS
（`e2e/online_s1_sync.mjs` 〜 `online_s5_fullmatch.mjs`）。

#### 検証結果サマリ

| シナリオ | 結果 |
|---|---|
| S1 カジュアルマッチ成立と同期 | ✅ マッチ成立/home・away割当/3ターンの盤面・スコア・ターン完全一致/片方確定時の相手待ち表示 |
| S2 Bot補完フォールバック | ✅ 30秒（COM_TIMEOUT_MS）+alarm間隔で発動、自分の確定のみでターン進行 |
| S3 フレンドマッチ | ✅ 招待コード方式/URL方式（?friend=）とも対戦成立、不正コードはエラー表示のみでクラッシュなし |
| S4 切断と再接続 | ✅ サイレント切断→自動再接続→盤面一致→試合継続。離脱（クローズフレームあり）は相手に切断バナー |
| S5 試合完走 | ✅ FULL TIMEまで完走、両者finished・スコア一致、リザルト遷移、/api/rankingへのELO反映（queue経由） |

#### 発見・修正したバグ（計8件、`e680aa0`/`d2d6807`）

**検証前はオンライン対戦は実質進行不能だった**（以下の1,2,3のいずれでも詰む）。

1. 🔴 client: useWebSocketのconnectが不安定な依存で切断→再接続を無限ループ（WSが永遠に繋がらない+429ストーム）
2. 🔴 client: APPLY_TURN_RESULTがturnPhaseを戻さず2ターン目以降INPUTに復帰不能
3. 🔴 server: 人間同士のMATCH_FOUNDにteamがなく両者が'home'扱い（away側が操作不能）
4. 🔴 server: Bot補完マッチがcomSessionToken認証を要求され常に403（一度も接続不能だった）
5. 🔴 client/server: TURN_INPUTのnonce衝突（同一ms二重送信）とsequence厳密+1検証により、一度ズレると全入力拒否の
   デッドロック → nonce衝突不能化+INPUT_REJECTEDにexpectedSequenceを同梱して自己回復
6. 🟠 client/server: ハーフタイム/試合終了がクライアントローカル乱数ATで判定されサーバーと乖離 →
   サーバー権威（HALFTIME/MATCH_ENDメッセージ、HALFTIMEに後半盤面同梱）に一本化
7. 🟠 client: 最終ターンのTURN_RESULT（リプレイ遅延適用）がMATCH_END後にstatusを巻き戻しFULL TIMEの結果ボタンが消える
8. 🟠 server: JWKS未知kidで再フェッチせず、鍵ローテーション直後に最大5分間全認証が401（本番でも起き得る実バグ）

#### 要決定リスト（2026-07-02更新: 2件解決済み、3件は保留継続）

- [x] **リロード/離脱後の試合復帰** — ✅ 解決（2026-07-02、`c6814a6`/`56ea725`）。オンラインマッチ開始時に
  進行中マッチ（matchId/team/mode）をsessionStorage（タブ単位=別タブ二重参加防止）へ保存し、起動時に
  `GET /match/:id` で生存確認（終了済みは黙って破棄）→ 生存時はマイページ最上部に「進行中の試合があります —
  復帰する/棄権する」バナー。復帰は既存RECONNECTフロー（リロードでリセットされるsequenceは
  INPUT_REJECTED+expectedSequenceで自動再同期・自動再送）、棄権は新設 `POST /match/:id/leave` で即時不戦敗
  （相手を30秒待たせない）。不戦敗猶予30秒は変更なし。E2E: S6/S4で検証
- [x] **casual/rankedの区別** — ✅ 解決（2026-07-02、`c6814a6`）。JOIN_QUEUEにmodeを追加し
  マッチングプールをモード別に分離（ranked同士/casual同士のみ）。カジュアルは `casual_` プレフィックスの
  matchIdで記録され、isRatedMatchがレーティング対象外として扱う（friend_/com_と同方式、スキーマ変更不要）。
  マイページ「ランダム対戦」=casual、ランクは「対戦へ」経由のみ。Bot補完は両モード有効で、Bot戦はもともと
  com_aiによりELO対象外（現仕様維持）。E2E: S6でプール分離とELO不変動、S5(ranked化)でELO変動を検証
- [ ] **オンラインのハーフタイム交代**（保留継続）: サーバーはハーフタイムで停止しない設計のため、交代パネルは表示せず演出のみとした。
  オンラインでハーフタイム交代をサポートするか（サーバーに停止フェーズを入れるか）要決定
- [ ] **試合時間表示のAT乖離**（保留継続）: クライアントの90+N表示はローカル乱数ATベースでサーバーのAT値と一致しない
  （進行自体はサーバー権威化済みで実害なし。表示同期はTURN_RESULTのhalf/isAdditionalTimeで可能）
- [ ] **サイレント切断の検知遅延**（保留継続）: TCPレベルの切断（電波断等）はサーバーが即座に検知できず切断通知が遅れる。
  PING/PONGタイムアウトによる能動検知を入れるか要決定

#### 再現手順（ローカルE2E）

```bash
# 1. src/.dev.vars を用意（PLATFORM_JWKS_URL=http://127.0.0.1:8790/jwks.json / issuer=https://fcms-e2e.local /
#    audience=football-chess-maniacs / CORS_ORIGIN=http://localhost:5173）
# 2. cd src && npx wrangler d1 migrations apply DB --local && npx wrangler dev --local --port 8799
# 3. VITE_API_BASE=http://localhost:8799 VITE_WS_BASE=ws://localhost:8799 npm run dev
# 4. node e2e/online_s1_sync.mjs（S2〜S5も同様。JWKSサーバーは各テストが自動起動）
```
