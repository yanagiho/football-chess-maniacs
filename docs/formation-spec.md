# 編成画面 仕様書 v2

## コンセプト

プレイヤーは**所有しているコマ（最大200個）の中から選んで配置**する。
コマはショップ（別画面）で購入して集める。
各コマにはポジションとコストが固定されており、編成画面では変更できない。

---

## コマの基本ルール

### 所有
- コマは最大200個まで所有可能
- 同じ種類（同ポジション・同コスト）のコマを複数所有できる
- 各コマに名前をつけられる

### コマの種類
ポジション8種 × コスト5段階 ＝ **40種類**

| ポジション | コスト1 | コスト1.5 | コスト2 | コスト2.5 | コスト3 |
|---|---|---|---|---|---|
| GK | ○ | ○ | ○ | ○ | ○ |
| DF | ○ | ○ | ○ | ○ | ○ |
| SB | ○ | ○ | ○ | ○ | ○ |
| VO | ○ | ○ | ○ | ○ | ○ |
| MF | ○ | ○ | ○ | ○ | ○ |
| OM | ○ | ○ | ○ | ○ | ○ |
| WG | ○ | ○ | ○ | ○ | ○ |
| FW | ○ | ○ | ○ | ○ | ○ |

### チーム編成ルール
- **スタメン：11枚**（GK 1人 + フィールドプレイヤー 10人）
- **スタメン合計コスト上限：16**
- **サブ（ベンチ）：9枚**（コスト制限なし）
- **合計：20枚**で1チーム
- GK以外のポジション制限なし（全員FWでもOK）
- GKは1人固定

### 試合中の選手交代ルール
- **交代機会：3回**（1回に複数人交代OK）
- **交代人数上限：合計5人まで**（サッカー準拠）
- 交代後も**ピッチ内の合計コストは16以内**であること
- サブからスタメンと同コスト以上のコマを出す場合、スタメン側を下げてコスト枠を確保する

### ランク帯システム（コスト判定の特殊ルール）

コストは3つのランク帯に分類される：

| ランク帯 | 含まれるコスト |
|---|---|
| 低ランク帯 | 1, 1.5 |
| 中ランク帯 | 2, 2.5 |
| 高ランク帯 | 3 |

コマ同士が対峙する判定（タックル・パスカット・シュート等）では、生のコスト差ではなく「有効差」を使用する：

| 対峙パターン | 例 | 有効差 |
|---|---|---|
| 同コスト | 1vs1, 2vs2, 3vs3 | 0 |
| 異ランク帯 | 1vs2, 1vs3, 2vs3 等 | 1 |
| 同ランク帯の0.5差 | 1vs1.5, 2vs2.5 | 2（最大） |

※0.5の差が同ランク帯のミラーマッチで最大効果を発揮する設計。

### 初期所持コマ（無課金スタート）

| ポジション | コスト | 枚数 |
|---|---|---|
| GK | 1 | 1 |
| DF | 1 | 4 |
| MF | 1 | 4 |
| FW | 1 | 2 |
| **合計** | — | **11枚** |

※初期はスタメン11枚のみ。ベンチなし（初心者に選手交代をさせないため）。
※SB・VO・OM・WGは初期所持なし。ショップで購入して入手する。
※ベンチ枠のコマはショップで買い足していく。

---

## 画面構成

### ヘッダー部
- クラブ名・監督名
- ユーザーID
- 総コスト表示：「コスト XX / 16」（リアルタイム更新）

### セーブスロット（タブ）
- 「システム1」「システム2」「システム3」…「＋」ボタン
- 最大10スロットまで保存可能
- 各スロットに名前をつけられる（任意）
- 「＋」で新規スロット追加

### フィールド部（メインエリア）
- サッカーフィールド風のビジュアル
- スタメン11枚のコマがフィールド上に配置される
- コマは**フィールド上の自由な位置に配置可能**（自由配置）
- 各コマの表示内容：
  - ポジション略称（GK, DF, SB, VO, MF, OM, WG, FW）
  - コスト数値
  - 名前（設定済みの場合）
  - ポジションごとの色分け

### サブエリア
- フィールド下部にベンチ9枠を表示
- サブのコマを並べて表示

### 手持ちコマ一覧（カードグリッド）

スタメンやサブのコマを入れ替えるときに表示される。所有コマをカードグリッド形式（2〜3列）で一覧表示する。

**カードの表示内容：**
- 顔イラスト（カードのメインビジュアル）
- ポジション略称（左上バッジ）
- コスト★マーク（右上）
- キャラクター名（下部）
- コスト枠装飾（1=無装飾, 1.5=銅枠, 2=銀枠, 2.5=金枠, 3=虹枠）

**カードの状態表示：**
- 使用中のコマ → 半透明 +「使用中」ラベル（選択不可）
- コスト16超過になるコマ → グレーアウト（選択不可）
- 選択可能なコマ → 通常表示、タップで即配置

**フィルター・ソート（画面上部）：**
- ポジションフィルター：アイコンボタン8つ + 「全部」
- コスト順ソート：昇順/降順切り替えボタン

### フッター部
- 「戻る」ボタン → マイページに戻る

---

## 操作フロー

### 新規編成の場合

1. 「＋」タブをタップして新規スロットを作成
2. デフォルトシステムを6つから選択（初期配置テンプレート）
   - 4-4-2 / 3-5-2 / 3-6-1 / 4-3-3 / 4-2-3-1 / 3-4-3
3. 選んだシステムに応じて、手持ちコマから自動的にコスト1のコマが配置される
4. ここから自由に編集する

### コマの入れ替え（フィールド上のコマを別のコマに変更）

1. フィールド上のコマをタップ → 選択状態になる
   - PC版：横のスペースにコマ詳細を大きく表示（顔イラスト、キャラクター名、背景テキスト、ポジション、コスト）
   - スマホ版：コマが拡大して略称・コスト等を表示
2. 「入れ替え」アクションを選択 → カードグリッドが開く
3. カードをタップ → 即入れ替え完了、コスト表示が更新される
   - コスト16超過のカードはグレーアウトで選択不可
   - 使用中のカードは半透明で選択不可

### コマの移動（フィールド上の位置変更）

1. コマを長押し（またはドラッグ）すると移動モードに入る
2. フィールド上の好きな場所にドラッグして配置
3. ドロップすると移動完了

**制約：**
- GKも含め全コマが自由に移動可能
- コマ同士は重ならない

※操作方法（タップ＝入れ替え、長押し＝移動）は要検討。逆のほうが直感的であれば変更可。

### サブ（ベンチ）の編集

1. サブエリアの枠をタップ → 選択状態になる
2. 「入れ替え」アクションを選択 → カードグリッドが開く（コスト制限なし、全カード選択可能）
3. カードをタップ → 即配置完了

### 名前の入力

1. コマの名前表示部分をタップ
2. テキスト入力欄が表示される
3. 任意の名前を入力（選手名など）

---

## デフォルトシステム

選択時に手持ちコマから自動配置される初期テンプレート。全コマはコスト1。

### 4-4-2

| # | ポジション | コスト | フィールド位置イメージ |
|---|---|---|---|
| 1 | GK | 1 | 最後方中央 |
| 2 | DF | 1 | 左CB |
| 3 | DF | 1 | 右CB |
| 4 | SB | 1 | 左SB |
| 5 | SB | 1 | 右SB |
| 6 | VO | 1 | 左中盤 |
| 7 | VO | 1 | 右中盤 |
| 8 | MF | 1 | 左MF |
| 9 | MF | 1 | 右MF |
| 10 | DF | 1 | 左FW（※FW未所持の場合DFで代替） |
| 11 | DF | 1 | 右FW（※FW未所持の場合DFで代替） |
| | **合計** | **11** | |

※手持ちにないポジションは所持コマで代替する。
※初期状態（無課金）ではOM・WG・FWを所持していないため、DFやMFで全枠を埋める。

### その他のシステム（3-5-2, 3-6-1, 4-3-3, 4-2-3-1, 3-4-3）
- 同様にポジションに応じて手持ちコマを自動配置
- 手持ちにないポジション（SB, VO, OM, WG等）の位置には、所持しているDF・MF等を代わりに配置
- コマ購入後にポジションを入れ替えることで本来のシステムが完成する

---

## セーブ/ロード

### 保存データ（スロットごと）

```json
{
  "slotId": 1,
  "slotName": "攻撃特化",
  "systemBase": "4-4-2",
  "starters": [
    {
      "pieceId": "piece_001",
      "position": "GK",
      "cost": 1,
      "name": "守護神太郎",
      "x": 250,
      "y": 450
    },
    ...
  ],
  "bench": [
    {
      "pieceId": "piece_012",
      "position": "OM",
      "cost": 3,
      "name": "ファンタジスタ"
    },
    ...
  ],
  "totalCost": 15.5
}
```

- x, y はフィールド上の自由配置座標
- スロットごとに独立して保存
- 最大10スロット

---

## バリデーション

| ルール | タイミング | 表示 |
|---|---|---|
| スタメン合計コスト ≤ 16 | コマ入れ替え時 | 超過するコマはグレーアウト |
| スタメン11枚 | 試合開始前 | 11枚未満なら警告 |
| GK 1枚必須 | 試合開始前 | GKがいなければ警告 |
| 同一コマの二重配置不可 | コマ選択時 | 使用中コマは「使用中」表示 |

---

## UI/UX要件

### PC版レイアウト
- フィールドを画面中央に配置
- 左右どちらかにコマ詳細パネル（選択時に顔イラスト・キャラクター名・背景テキスト・ポジション・コストを大きく表示）
- カードグリッドはモーダルまたは右パネルに表示
- セーブスロットのタブは画面上部

### スマホ版レイアウト
- フィールドが画面全体を占める
- コマをタップすると拡大表示（略称・コスト表示）
- カードグリッドは画面下部からスライドアップ（2列表示）
- セーブスロットのタブは画面上部（横スクロール）

### 共通
- フィールドはサッカーピッチ風デザイン（縦長・ライン表示）
- コマはポジションごとに色分けされた形状
- コスト枠装飾：1=無装飾, 1.5=銅枠, 2=銀枠, 2.5=金枠, 3=虹枠
- 移動中のコマは視覚的に分かりやすく（拡大、発光、影など）
- コスト超過時は数値を赤字で警告表示
- 長押し判定は0.5秒以上
- スタメンは常に11枚（外す機能なし）
